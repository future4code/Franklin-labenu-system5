export type user = {
    id: string
    name: string
    email: string
    birthDate: string
    class_id: string
 }
 
 export type schoolClass = {
    id: string
    name: string
    module: string
 }

 export type authData = {
    id: string
 }